# =================================================================
# NPM/Yarn 配置 - 优化跨平台构建兼容性
# =================================================================

# 确保平台特定依赖正确安装
optional=true
include=optional

# 使用legacy peer deps解决依赖冲突
legacy-peer-deps=true

# 避免审计问题影响构建
audit=false
fund=false

# 让npm自动选择平台依赖，不强制指定
target_platform=
target_arch=

# =================================================================
# 构建平台特定说明:
# - 这个配置文件同时支持 npm 和 yarn
# - 让rollup自动选择正确的平台依赖
# - 避免手动指定平台包导致的兼容性问题
# =================================================================
