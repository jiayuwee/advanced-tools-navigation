# NPM 配置文件 - 确保跨平台兼容性

# 启用可选依赖，支持平台特定的二进制文件（如 Rollup）
optional=true

# 使用精确版本匹配
save-exact=true

# 禁用包锁定文件中的平台特定依赖
target_platform=
target_arch=
target_libc=

# 设置注册表
registry=https://registry.npmjs.org/

# 构建配置
rebuild-bundle=false
