// minimal stub for additional tools data

// Export an empty array by default. If your project requires static initial data,
// replace this file with the full dataset.
export const additionalTools: unknown[] = [];

export default additionalTools;
