name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    name: Test and Build
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      - name: Run type checking
        run: npm run type-check

      - name: Build project
        run: npm run build

      - name: Test build output
        run: |
          if [ ! -d "dist" ]; then
            echo "❌ Build failed: dist directory not found"
            exit 1
          fi
          if [ ! -f "dist/index.html" ]; then
            echo "❌ Build failed: index.html not found"
            exit 1
          fi
          echo "✅ Build successful"

  # Netlify 会自动部署，这里只是验证构建成功
  deploy-check:
    runs-on: ubuntu-latest
    needs: test-and-build
    if: github.ref == 'refs/heads/main'
    name: Deployment Status Check
    steps:
      - name: Deployment notification
        run: |
          echo "🚀 Code pushed to main branch"
          echo "📦 Netlify will automatically deploy to: https://ramusi.cn"
          echo "⏱️  Deployment usually takes 1-3 minutes"
          echo "🔗 Check deployment status at: https://app.netlify.com/sites/spiffy-torrone-5454e1/deploys"
